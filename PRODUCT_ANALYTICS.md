# CRM 产品运营数据驾驶舱

## 本期定位

该驾驶舱用于演示产品运营如何从体验行为识别迭代机会。它合并两类数据：

- 可重复生成的近 7 天演示基线；
- 当前浏览器内的实时匿名行为。

这不是生产分析系统。数据不出浏览器，不接入 D1、第三方分析平台或客户业务系统。

## 数据边界

事件固定包含：发生时间、匿名会话、角色类型、模块、动作、结果和白名单枚举属性。

不会采集或写入本地埋点：姓名、手机号、客户 ID、搜索词、报价金额、小万对话或任何自由文本。

本机事件存储在 `localStorage`，最多保留 500 条。损坏、未知版本或不可用的缓存会自动忽略；“重置本机数据”仅清除这些真实本机事件，不影响工作台偏好，也不影响演示基线。

## 指标口径

- 活跃会话：时间范围内有行为的匿名业务角色会话，不代表跨设备真实用户数。
- 关键动作率：完成客户查看、报价生成/分享、试驾或订单交付等关键动作的会话占比。
- 工作台参与率：进入工作台后继续完成工作台动作的会话占比。
- 智能补位采纳率：工作台触达后开启或完成自动转入的会话占比。
- 业务行为漏斗：进入工作台 → 查看客户 → 打开报价 → 生成/分享报价，仅为行为代理指标，不代表真实成交。

产品运营自己的驾驶舱操作会被单独标记为 `operations`，不进入业务角色的指标、漏斗、趋势或功能采用排行。

## 后续演进

统一事件模型已与服务端无关。若未来需要多设备汇总，可在不改变前端事件字段的前提下，将采集出口迁移到 Cloudflare Worker + D1，并补充账号、权限、留存与导出能力。
