# CRM 产品运营驾驶舱 2.0

## 定位与边界

这是一个用于演示产品运营方法的本地驾驶舱，不是生产分析系统。它将**56 天可重复演示基线**与**当前浏览器的实时匿名行为**合并展示；选择“仅本机”时只查看本机行为。数据不会离开浏览器，未接入 D1、第三方分析平台或客户业务系统。

产品运营账号产生的浏览行为会单独记入数据健康，永远不进入业务角色的会话、漏斗、趋势或功能采用指标。

本期绝不采集：姓名、手机号、客户 ID、报价金额、搜索关键词、小万对话文本，或任何自由文本。

## 事件模型（schemaVersion: 2）

每条事件只包含以下受控字段：

- 匿名浏览器标识与 30 分钟滚动匿名会话；
- 体验角色、角色旅程、模块、动作、状态和数据来源；
- 白名单枚举属性：入口位置、工具类型、旅程阶段、推荐模式、配置动作。

状态语义是严格的：

| 状态 | 含义 | 例子 |
| --- | --- | --- |
| `viewed` | 页面或信息已呈现 | 打开客户 360、查看工具详情 |
| `started` | 流程已发起 | 预约试驾、发车、建单、交车、打开报价 |
| `succeeded` | 可由当前 UI 验证的站内行为成功 | 报价卡生成、工作台拖拽、添加工作必备 |
| `external_handoff` | 已交接给外部通道 | 点击 WhatsApp 外发；不代表客户收到或成交 |
| `failed` | 可观测的本地操作失败 | 报价卡复制失败 |
| `cancelled` | 用户主动取消 | 关闭未完成的报价卡 |

试驾完成、订单完成、客户阅读与真实成交目前不可观测，因此本期不展示这些数值。

本机事件存于 `localStorage`，最多保留 500 条。未知、损坏和旧版缓存均会安全降级；旧版 v1 仅用于健康提示，不进入 v2 严格漏斗。重置只清除本机埋点，不影响工作台偏好或演示基线。

## 唯一官方指标口径

| 指标 | 严谨定义 | 标签 |
| --- | --- | --- |
| 有效工作会话 | 工作台访问后 30 分钟内，按顺序完成至少一个核心工作动作 | 可验证行为 |
| 工作推进率 | 同一会话按顺序完成“工作台 → 关键事项/今日行程 → 下游模块” | 业务过程代理 |
| 销售下一步意图率 | 产品专家按顺序完成“客户 360 → 报价 → 报价卡 → 外发意图” | 业务过程代理 |
| 智能推荐采纳率 | `recommendation_shown → recommendation_accepted`；自动执行单列 | 可验证行为 |
| 工具激活率 | 添加工作必备后 7 天内，以同一匿名浏览器再次启动工具 | 业务过程代理 |
| 中位推进耗时 | 严格完成工作推进路径的会话，从工作台到下游模块的中位耗时；仅 n≥20 展示 | 业务过程代理 |

“顺序”意味着相同匿名会话、实际时间递增、前一步存在后才可计入下一步；绝不用不同事件集合的交集代替漏斗。

## 驾驶舱视角

### 业务旅程（默认）

- 默认查看所有业务角色的通用工作推进路径；选择角色后展示专属旅程。
- 产品专家：工作台 → 客户 360 → 报价 → 报价卡生成 → 外发意图。
- 店长 / 大区：工作台 → 经营模块进入 → 决策流程发起。
- 售后 / 交付：工作台 → 服务或订单模块进入 → 流程发起。
- 趋势使用会话量柱图和推进率折线的分层小图；7 天按日、28 天按周聚合，不使用双 Y 轴。

### 功能采用

- 功能排行按独立有效会话的触达率与使用深度排列，不按原始点击量。
- 应用中心查看“打开 → 详情 → 添加工作必备 → 7 天内再次启动”。
- AI 采用“推荐展示 → 手工接受 / 自动执行 → 转入事项被打开”；手工与自动永远分开。
- 个性化查看工作必备配置率、面板排序率和配置后复用率。

## 运营结论门槛

每条结论固定呈现“数据证据 → 可解释影响 → 待验证假设 → 建议实验 → 主指标 + 护栏指标”。

只有同时满足以下条件才展示：样本量至少 12、事件结构完整、相对前一周期出现至少 5 个百分点的方向变化。结论统一使用“相关信号”，不将相关性伪装成因果。

## 下一阶段：Cloudflare Worker + D1

页面继续部署在 Cloudflare Pages。需要跨设备汇总时，可保持同一事件字典，将采集出口接入 Worker：

1. Worker 校验 schema 与白名单，拒绝敏感或未知字段；
2. D1 保存 90 天去标识原始事件、13 个月日聚合；
3. 无登录体系前，只报告匿名浏览器 / 会话，不报告真实用户数或跨设备留存。
